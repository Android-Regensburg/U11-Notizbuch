<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity"
    style="@style/AppTheme">

    <!-- Include layout files holding the fragments. On wide (e.g. 600dp) displays, both
         fragments should be shown at the same time, on smaller displays only the list
         fragment should be shown (the other fragment resides in the ContentActivity in
         this case). -->
    <include
        layout="@layout/content_main"
        android:id="@+id/content_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/content_main"
        app:layout_anchor="@id/content_main"
        app:layout_anchorGravity="bottom|left|end"
        android:backgroundTint="@color/colorPrimary"
        android:tint="@color/colorBackground"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="10dp"
        android:src="@android:drawable/ic_input_add"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
